version: "3"

services:
  react-app:
    image: medicords.azurecr.io/react-app:latest
    container_name: react-app
    restart: unless-stopped
    ports:
      - "80:80" #HTTP
      - "443:443" #HTTPS caddy
    networks:
      - mern-app
    volumes:
      - caddy-data:/data
      - caddy-config:/config
  api-server:
    image: medicords.azurecr.io/api-server:latest
    container_name: api-server
    restart: unless-stopped
    env_file:
      - ./config.env
    ports:
      - "4000:4000"
    networks:
      - mern-app

volumes:
  caddy-data:
    driver: local
  caddy-config:
    driver: local
  mongo-data:
    driver: local
networks:
  mern-app:
    driver: bridge


      
